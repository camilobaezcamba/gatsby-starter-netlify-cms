/*
const IntlProvider = require('react-intl')
exports.wrapRootComponent = (Root) => {
  const locale = 'en';
  const Wrapper = () => (
    <IntlProvider locale={locale}>
        <Root />
    </IntlProvider>
  )
  return Wrapper;
}
*/